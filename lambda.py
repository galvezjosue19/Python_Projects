#This is a lambda function and doesnt have a name 'un-named function' limited to a single expression, no statements.
doubleIt = lambda x: x*2  

print(doubleIt(7))

#This is the regular way to define a function.
def doubleItFunct(x):
    return x*2
print(doubleItFunct(7))

my_list = [1, 5, 4, 6, 8, 11, 3, 12]